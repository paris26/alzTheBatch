# Alzheimer's MRI Classification Experiment

This project is the implementation of an experiment for a paper. The experiment
includes two model architectures: Swin and ResNet. The models were created
through Jupyter notebooks, and a standalone Python file was added for
reproducibility.
